# Bindings

This directory contains language bindings and related examples.

- `python/`: Python package, tests, notebooks, and example scripts
- `node/`: Node.js binding entrypoint
- `go/`: Go binding wrapper
- `java/`: Java native bridge
- `csharp/`: C# native bridge

Use these bindings alongside the core CLI workflows documented in `docs/README.md`.
