# Baseline governance bundle — convert to policy JSON per docs/governance/compliance-test-suite.md and docs/policy-engine.md
name: default
version: 1.0.0
controls:
  allowed_models:
    - gpt-4o-mini
    - demo
  allowed_seeds:
    - 1
    - 42
    - 1337
  allowed_external_calls:
    - https://api.openai.com
    - https://api.anthropic.com
  required_evidence_fields:
    - trace_id
    - policy_id
    - tenant_id
