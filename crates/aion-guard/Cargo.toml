[package]
name = "aion-guard"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "aion-guard"
path = "src/main.rs"

[dependencies]
aion-cli = { path = "../aion-cli" }
clap = { version = "4", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
